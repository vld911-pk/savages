import styled from 'styled-components';

const NavInformWrapper = styled.div`
    dipslay : flex;
    flex-direction: row;
    text-align : center;
    padding : 5px;
    justify-content : space-between;
`
export default NavInformWrapper;